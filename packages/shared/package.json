{
  "name": "@smart-hospital/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit -p tsconfig.json",
    "test": "node --test",
    "format": "prettier --check \"src/**/*.ts\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
